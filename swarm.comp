#version 460
layout(local_size_x = 256) in;

void main() {
    // LOBOTOMIZED.
    // The GPU compute is disabled. The CPU AVX2 Fork-Join pool 
    // is now the sole authoritative owner of the particle memory.
    return;
}
